# Sorftware-Engineering-Course
用户账户模块（User Module）说明
概述
本模块 (UserController) 负责处理系统中所有与用户账户相关的操作，包括 注册、登录、找回密码、修改密码、登录状态校验及退出登录。系统采用基于 Token 的无状态认证机制，结合 Redis 实现会话管理，支持多方式登录（用户名/邮箱/手机号），并具备基础的安全防护（如 RSA 解密密码、审核状态校验等）。
注意：本系统区分三类用户角色（患者、医生、管理员），用户组（user_group）信息用于权限控制和审核流程。
